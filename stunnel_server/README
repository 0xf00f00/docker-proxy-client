# Stunnel Server Configuration

This directory contains the configuration files for the stunnel server. More information on the [stunnel website](https://www.stunnel.org/config_unix.html).

## stunnel.conf

**IMPORTANT**: Make sure to add `foreground = yes` to the configuration file so that the container runs properly.